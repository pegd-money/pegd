{
  "name": "pegd-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Pegd -- Solana stablecoin issuance framework. TypeScript workspaces for the SDK, CLI, and support packages.",
  "scripts": {
    "typecheck": "pnpm -r --parallel typecheck",
    "build": "pnpm -r --parallel build",
    "clean": "pnpm -r --parallel clean"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
